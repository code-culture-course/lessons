"""
Упражнение 10: Безопасные операции с файлами

ЦЕЛЬ: Создать вспомогательные функции для безопасного чтения и записи файлов.

ТРЕБОВАНИЯ:
1. Реализовать функцию read_file_safely(filename):
   - Пытается прочитать файл
   - Возвращает содержимое при успехе
   - Возвращает None, если файл не существует или есть ошибки
   - Выводит соответствующее сообщение об ошибке

2. Реализовать функцию write_file_safely(filename, content):
   - Пытается записать содержимое в файл
   - Возвращает True при успехе
   - Возвращает False при ошибке
   - Выводит соответствующее сообщение об ошибке

3. Протестировать обе функции

ПРИМЕР ВЫВОДА:
    Попытка чтения 'data.txt'...
    Файл не найден: data.txt
    
    Попытка записи в 'output.txt'...
    Успешно записано в output.txt
    
    Попытка чтения 'output.txt'...
    Содержимое файла: Hello, World!

ПОДСКАЗКИ:
- Используйте try/except с FileNotFoundError
- Используйте try/except с общим Exception для ошибок записи
- Возвращайте None или False для обозначения ошибки
- Выводите ясные сообщения об ошибках
"""


def read_file_safely(filename):
    """
    Безопасно читает содержимое файла.
    
    Аргументы:
        filename: Файл для чтения
        
    Возвращает:
        str: Содержимое файла, или None при ошибке
    """
    # TODO: Реализуйте эту функцию
    # Используйте try/except с FileNotFoundError
    # Верните содержимое файла при успехе, или None при ошибке
    pass


def write_file_safely(filename, content):
    """
    Безопасно записывает содержимое в файл.
    
    Аргументы:
        filename: Файл для записи
        content: Текстовое содержимое для записи
        
    Возвращает:
        bool: True при успехе, False в противном случае
    """
    # TODO: Реализуйте эту функцию
    # Используйте try/except с Exception
    # Верните True при успехе, или False при ошибке
    pass


# Тестируйте ваши функции
if __name__ == "__main__":
    # Тест чтения несуществующего файла
    print("Попытка чтения 'data.txt'...")
    content = read_file_safely("data.txt")
    print()
    
    # Тест записи
    print("Попытка записи в 'output.txt'...")
    success = write_file_safely("output.txt", "Hello, World!")
    print()
    
    # Тест чтения существующего файла
    if success:
        print("Попытка чтения 'output.txt'...")
        content = read_file_safely("output.txt")
        if content:
            print(f"Содержимое файла: {content}")
